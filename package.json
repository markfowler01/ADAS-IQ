{
  "name": "adas-iq",
  "version": "1.0.0",
  "description": "ADAS IQ — Kinetic calibration report extractor and Zoho invoice creator",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "cd client && npm run dev",
    "build": "cd client && npm run build",
    "deploy:staging": "cd client && npm run build && cd .. && ~/.npm-global/bin/catalyst deploy",
    "deploy:prod": "cd client && npm run build && cd .. && ~/.npm-global/bin/catalyst deploy --env Production",
    "server": "node --watch server/index.js",
    "start": "node server/index.js"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.30.0",
    "axios": "^1.7.2",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "express-session": "^1.19.0",
    "multer": "^1.4.5-lts.1",
    "pdfkit": "^0.18.0"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
